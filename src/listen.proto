syntax = 'proto3';

package message;

message Listen {
  string user_id = 1;
}

message ListenResponse {
  enum ActionType {
    STATE_CHANGE = 0;
  }

  string     user_id = 1;
  ActionType type    = 2;
  bytes      meta    = 3;
  bool       ok      = 4;
  string     error   = 5;
}
